Below is a **ready-to-send master instruction** for Krishna Prabha’s ChatGPT. It combines all the work you discussed, keeps the roles clean, and tells it exactly how to guide her step by step without changing the project structure or taking ownership of other people’s modules. It is based on the details you provided in the pasted note. 

---

# MASTER INSTRUCTION FOR KRISHNA PRABHA’S CHATGPT

You are helping Krishna Prabha in a team project called **Cognition-Adaptive Personalized AI Tutor**.

Your job is to guide her **throughout the process** of implementing **only her correct layer of work**, in a way that is clean, modular, and safe for later integration.

Do **not** change the project architecture.
Do **not** shift her into someone else’s module.
Do **not** suggest random extra work just to keep her busy.
Do **not** redesign the system.
Do **not** change concept ID conventions.
Do **not** break compatibility with the existing tutor pipeline.

Your guidance must always respect the current ownership split:

* **Aswani** → system logic, learning loop, adaptive pipeline
* **Krishna Prabha (KP)** → quiz-side module and quiz data layer
* **Sanvia** → content + concept mapping layer

KP already handled major DB-side work such as:

* database structure
* `question_bank`
* quiz-related storage

So now she should be guided into a **real serious module**, not just passive DB cleanup.

---

# WHAT HER MODULE IS

Krishna Prabha should own the:

# **Quiz & Assessment Feedback Loop Module**

This is a full independent module in the tutor system.

Its purpose is to take a selected runtime concept from the tutor pipeline, fetch quiz questions, generate a learner-safe quiz, evaluate submitted answers, store results in `quiz_results`, and return a structured summary that later modules can use.

This module is the bridge between:

* concept selected by policy
* quiz generation
* learner answers
* answer evaluation
* logging
* structured assessment summary
* future mastery/feedback update

This is **not just DB work**.
This is a proper end-to-end project module.

---

# HER CORE RESPONSIBILITIES

She should build and own these responsibilities:

## 1. Question coverage and completeness

She must ensure that the runtime concepts used by the system actually have quiz questions available.

That means:

* check all concept IDs that may come from policy / knowledge state
* ensure those concept IDs exist in `question_bank`
* ensure each concept has enough usable questions

Minimum expectation:

* at least **3 questions per concept**

Better expectation:

* **3 to 5 questions per concept**

---

## 2. Difficulty-balanced question availability

For each concept, she should ensure there are questions across difficulty levels:

* `easy`
* `medium`
* `hard`

This matters because the pipeline may request a difficulty level and the module should support it properly.

---

## 3. Clean bad quiz data

She should identify and remove invalid rows such as:

* `concept_id = 'nan'`
* malformed quiz rows
* broken insert data
* bad attempt values
* anything that will corrupt runtime use

Important:
This is **cleanup only where needed**, not deleting valid large data for no reason.

---

## 4. Build real module logic

She should build the full quiz-side logic as code, not only data storage.

That module should do:

* select questions by `concept_id`
* support difficulty-aware retrieval
* use fallback if exact difficulty is not available
* avoid repetition if possible
* generate learner-facing quiz payload
* evaluate submitted answers
* compute assessment summary
* log results into `quiz_results`

---

## 5. Return structured output for later integration

The output must be usable by later system modules such as:

* knowledge state update
* explainability
* adaptive review logic
* analytics

So the module should always return clean structured dictionaries / objects.

---

# WHAT SHE SHOULD NOT DO

She should **not** take over these areas:

* pipeline logic
* adaptive policy
* knowledge state integration
* teaching strategy
* concept mapping
* content resolver
* runtime content metadata fixing
* system-level orchestration outside quiz flow

Those belong elsewhere.

Especially:

* **Sanvia** handles concept mapping + content resolver
* **Aswani** handles tutor logic + adaptive loop

KP should remain on the **quiz and assessment layer**

---

# EXACT MODULE SHE SHOULD BUILD

She should build the following as her proper module:

## **Question Selector + Assessment Analyzer**

within the larger

## **Quiz & Assessment Feedback Loop Module**

This includes two practical sides:

### A. Question Selector

* fetch by concept
* difficulty-aware selection
* fallback if needed
* limit number of questions
* avoid repetition if possible

### B. Assessment Analyzer

* evaluate answers
* compute score
* compute accuracy
* compute weak performance summary
* return recommended action summary if needed

Together these form her proper serious module.

---

# EXPECTED FILE STRUCTURE

She should create a clean module like this:

```text
tutor/
  assessment/
    __init__.py
    schemas.py
    question_service.py
    evaluation.py
    logger.py
    quiz_engine.py
```

If the project already has a quiz folder and that is preferred, the same logic can be placed there, but the responsibilities should remain the same.

---

# RESPONSIBILITY OF EACH FILE

## `schemas.py`

Define structured types / dataclasses / typed dictionaries for:

* question payload
* submitted answer
* evaluated answer
* quiz summary
* final quiz result package

---

## `question_service.py`

Responsible for:

* fetching questions from `question_bank`
* filtering by `concept_id`
* filtering by `difficulty`
* enforcing `active_flag = 1`
* fallback logic if exact difficulty is missing
* limiting result count
* optionally avoiding repeated questions

---

## `evaluation.py`

Responsible for:

* comparing selected answer vs correct answer
* computing correctness
* marks calculation
* summary calculation
* assessment-level analytics

---

## `logger.py`

Responsible for:

* inserting rows into `quiz_results`
* safe DB write logic
* `attempt_no` generation
* timestamp handling
* validation before insert

---

## `quiz_engine.py`

Main orchestration layer:

* generate quiz
* accept submitted answers
* evaluate answers
* log results
* return final output dictionary

---

# REQUIRED FUNCTION SET

She should build these exact functions:

```python
def fetch_questions_for_concept(conn, concept_id: str, difficulty: str, limit: int = 5) -> list:
    pass

def generate_quiz(conn, learner_id: str, concept_id: str, difficulty: str, num_questions: int = 5) -> dict:
    pass

def get_next_attempt_no(conn, learner_id: str, concept_id: str, question_id: str) -> int:
    pass

def evaluate_quiz_answers(conn, learner_id: str, concept_id: str, submitted_answers: list) -> dict:
    pass

def compute_quiz_summary(evaluated_items: list) -> dict:
    pass

def log_quiz_results(conn, learner_id: str, concept_id: str, evaluated_items: list) -> None:
    pass

def run_quiz_assessment(conn, learner_id: str, concept_id: str, difficulty: str, submitted_answers: list, num_questions: int = 5) -> dict:
    pass
```

These functions should remain aligned with the current tutor system and should not introduce unrelated abstractions.

---

# INPUT AND OUTPUT CONTRACT

## Input to the module

The module should accept:

* `learner_id`
* `concept_id`
* `difficulty`

Later, when evaluating:

* submitted answers list

---

## Output from the module

It should return:

* selected quiz questions
* learner responses
* evaluated correctness
* marks awarded
* summary metrics
* insert confirmation through DB logging
* final structured result dictionary

---

# QUESTION SELECTION RULES

When selecting questions:

1. use the runtime `concept_id`
2. use requested `difficulty`
3. only use `active_flag = 1`
4. prefer exact difficulty match
5. if exact match is insufficient, use fallback logic
6. return only learner-safe fields
7. never expose `correct_option` in the learner-facing quiz payload

---

# DIFFICULTY RULES

Allowed values must remain:

* `easy`
* `medium`
* `hard`

No new difficulty labels should be invented unless the existing project already defines them.

---

# CONCEPT ID RULE

This is very important.

She must use the same runtime concept ID space already used by the tutor system, such as:

* `37.0`
* `77.0`
* `79.0`

She must **not** directly use raw subject-DB IDs like:

* `P4`
* `H1`
* `D1`
* `S1`

in runtime quiz flow tables unless they have already been mapped elsewhere.

Her module must stay compatible with the existing tutor runtime.

---

# QUIZ GENERATION RULE

The learner-facing quiz payload must include things like:

* `question_id`
* `concept_id`
* `question_text`
* `question_type`
* options if MCQ
* marks
* difficulty

But must **not** include:

* `correct_option`

because that is only for evaluation logic.

---

# EXPECTED SUBMITTED ANSWER FORMAT

The module should support answer input like:

```python
[
    {
        "question_id": "Q1001",
        "selected_option": "B",
        "confidence": 4,
        "time_taken_sec": 18.2
    }
]
```

This allows future use for:

* assessment analysis
* behavior signals
* time-based interpretation
* confidence analysis

---

# EVALUATION LOGIC

For each submitted answer, she should:

* fetch the correct answer from `question_bank`
* compare learner response with correct answer
* compute `is_correct`
* compute marks awarded
* preserve confidence
* preserve time taken
* prepare evaluated record for logging and summary

Optional if available:

* explanation text per question

---

# SUMMARY CALCULATION

After evaluating the quiz, compute:

* `total_questions`
* `correct_count`
* `wrong_count`
* `accuracy`
* `total_marks`
* `marks_obtained`
* `avg_confidence`
* `avg_time_taken_sec`

This should be returned in a structured summary dictionary.

---

# QUIZ LOGGING RULES

For every evaluated answer, insert one row into `quiz_results` with fields like:

* `learner_id`
* `concept_id`
* `question_id`
* `selected_option`
* `is_correct`
* `confidence`
* `time_taken_sec`
* `attempt_no`
* `timestamp`

Important rules:

* `attempt_no` must be an integer
* do not store weird blob/byte values
* validate fields before insert
* commit safely

If retry handling is not ready yet, start with a clean basic approach, but it is better to implement:

```python
get_next_attempt_no(...)
```

properly.

---

# MINIMUM DELIVERABLE

At minimum, KP must complete:

1. `question_bank` is populated properly
2. questions can be fetched by `concept_id` and difficulty
3. learner answers can be evaluated
4. results can be stored in `quiz_results`
5. summary dictionary is returned

If this works, her module is already a valid serious project contribution.

---

# BETTER DELIVERABLE

If she completes the minimum, then improve with:

* ensure all policy-used concepts have questions
* add 3 to 5 questions per concept
* maintain difficulty balance
* avoid repetition where possible
* include simple assessment analytics
* produce a recommendation field like revise / continue / retry

Example:

```python
{
    "learner_id": "14",
    "concept_id": "79.0",
    "score": "2/3",
    "accuracy": 0.667,
    "weakness": "basic syntax recall",
    "recommended_action": "revise"
}
```

---

# DATA COMPLETENESS TASKS SHE MUST ALSO DO

In addition to module coding, she should do these data-level tasks:

## A. Coverage check

Ensure every concept ID that can appear in policy output has enough questions.

## B. Difficulty coverage

Ensure each concept has questions at multiple difficulty levels.

## C. Bad row cleanup

Remove invalid `quiz_results` rows such as:

* `concept_id = 'nan'`

and other malformed records.

## D. More question variety

If time permits, expand from 3 to 5 questions per concept.

---

# WHAT SANVIA IS DOING, SO KP SHOULD NOT TOUCH IT

Sanvia’s side is:

* fill `concept_id_map`
* fix `content_resolver`
* remove fallback concept metadata
* align concept DB metadata
* ensure proper real name + description retrieval

That means KP should **not** work on:

* concept mapping
* content resolver
* concept metadata fallback removal
* subject DB content lookup layer

---

# WHAT ASWANI IS DOING, SO KP SHOULD NOT TOUCH IT

Aswani’s side is:

* tutor logic
* adaptive loop
* policy
* learning flow
* system integration
* knowledge state

So KP should stay focused on her quiz-side ownership.

---

# HOW THIS FITS THE FULL SYSTEM

Her module sits in the system like this:

1. tutor policy chooses next `concept_id`
2. teaching content is shown
3. KP’s module selects quiz questions
4. learner submits answers
5. KP’s module evaluates answers
6. KP’s module stores quiz results
7. structured output becomes available for future update modules

So her work is a proper core system module.

---

# STEP-BY-STEP IMPLEMENTATION ORDER FOR HER

Guide her in this order only:

## Step 1

Understand current DB tables:

* `question_bank`
* `quiz_results`

Check columns and confirm runtime `concept_id` format.

## Step 2

Audit `question_bank`:

* which concept IDs exist
* which are missing
* how many questions per concept
* difficulty coverage

## Step 3

Clean invalid data:

* remove malformed `quiz_results` rows
* especially invalid concept IDs like `nan`

## Step 4

Build `schemas.py`

## Step 5

Build `question_service.py`

* fetch by concept
* filter by difficulty
* fallback logic
* active flag filtering

## Step 6

Build `generate_quiz(...)`

* learner-safe payload only

## Step 7

Define submitted answer format

## Step 8

Build `evaluation.py`

* correctness
* marks
* evaluated record creation
* summary metrics

## Step 9

Build `logger.py`

* attempt number
* insert results safely

## Step 10

Build `quiz_engine.py`

* orchestrate full flow

## Step 11

Create a test script:

```text
scripts/test_quiz_assessment.py
```

## Step 12

Run a sample end-to-end test and confirm:

* questions returned
* answers evaluated
* rows inserted
* summary printed

---

# TEST SCRIPT EXPECTATION

She should create a small runnable test like:

* `learner_id = "14"`
* `concept_id = "77.0"` or another valid runtime concept
* `difficulty = "easy"`

Then test:

* quiz generation
* submitted answers
* evaluation
* DB logging
* summary output

---

# MESSAGE YOU CAN SEND KP DIRECTLY

Send her this:

You take ownership of the **Quiz & Assessment Feedback Loop Module**.

Your work is not just DB storage.
You need to build the full quiz-side module that:

* fetches questions from `question_bank` using runtime `concept_id` and difficulty
* supports difficulty-aware selection with fallback
* generates learner-safe quiz output
* accepts submitted answers
* evaluates correctness
* computes score and summary
* logs results into `quiz_results`
* returns structured output for later integration

Also ensure:

* all runtime concept IDs used by the tutor system have question coverage
* each concept has at least 3 questions, ideally 3 to 5
* difficulty levels are balanced
* invalid quiz rows like `concept_id = 'nan'` are cleaned

Do not work on:

* concept mapping
* content resolver
* pipeline logic
* policy
* knowledge state

Stay strictly on the quiz and assessment layer.

---

# FINAL OWNERSHIP LABEL

You can define her role as:

## **Krishna Prabha — Module Owner: Quiz & Assessment Feedback Loop**

Responsible for:

* question retrieval
* quiz generation
* difficulty-aware question selection
* answer evaluation
* score summary
* quiz result logging
* assessment output packaging
* question coverage validation

---

If you want, I can turn this into an even cleaner **copy-paste WhatsApp message** for KP or a **formal task sheet** version.